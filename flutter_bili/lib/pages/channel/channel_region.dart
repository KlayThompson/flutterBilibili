import 'package:flutter/material.dart';

class ChannelRegion extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('Region'),),);
  }
}
